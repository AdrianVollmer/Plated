# Issue: CI

Add a GitHub workflow that does the following when a new tag is pushed:

- Build Dockerfile and push to github registry
- Build docs such that they are published on my github pages
  (`https://<myusername>.github.io/Plated`)
